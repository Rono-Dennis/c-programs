 #include <stdio.h>
void display(int num1, int num2) {
  printf("%d\n", num1);
  printf("%d\n", num2);
}

int main() {
  int numArray[] = {2, 8, 4, 12};

  // pass second and third elements to display()
  display(numArray[1], numArray[2]); 
  return 0;
}
